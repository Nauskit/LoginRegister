package GUIs;

import javax.swing.*;

public class PageGuis extends JFrame {
    public PageGuis(String title){
        super(title);

        setSize(540,680);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setLayout(null);
        setLocationRelativeTo(null);
        setResizable(false);
    }
}
